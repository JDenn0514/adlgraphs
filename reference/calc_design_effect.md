# Calculate design effect (DEFF) for weighted data

Calculate design effect (DEFF) for weighted data

## Usage

``` r
calc_design_effect(weights)
```

## Arguments

- weights:

  Vector of weights

## Value

Design effect value
