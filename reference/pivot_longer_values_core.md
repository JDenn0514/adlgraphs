# pivot_longer_values main interal helper function

Takes a data frame, and pivots it long-ways while preserving label
attributes.

## Usage

``` r
pivot_longer_values_core(data, cols, names_to, values_to, name_label, ...)
```
