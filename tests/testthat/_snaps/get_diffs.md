# error when x is not numeric

    Code
      test_data %>% get_diffs(edu_f2, pid_f3)
    Condition
      Error in `get_diffs()`:
      ! `x` and must be of class `numeric`
      i `edu_f2` is of class factor

